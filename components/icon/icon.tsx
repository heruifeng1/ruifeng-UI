import React, { FunctionComponent } from 'react';

interface OwnProps {}

type Props = OwnProps;

const Icon: FunctionComponent<Props> = (props) => {

  return (<span>1</span>);
};

export default Icon;
